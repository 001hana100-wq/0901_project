const root=document.documentElement;const themeBtn=document.querySelector('.theme-toggle');const saved=localStorage.getItem('blog-theme');root.dataset.theme=saved||'light';themeBtn?.addEventListener('click',()=>{root.dataset.theme=root.dataset.theme==='dark'?'light':'dark';localStorage.setItem('blog-theme',root.dataset.theme)});
const menuBtn=document.querySelector('.menu-toggle');const mobileNav=document.querySelector('.mobile-nav');menuBtn?.addEventListener('click',()=>{const open=mobileNav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open));menuBtn.textContent=open?'×':'☰'});document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());
const search=document.querySelector('[data-search]');search?.addEventListener('input',()=>{const q=search.value.trim().toLowerCase();let visible=0;document.querySelectorAll('.post-item').forEach(item=>{const show=item.dataset.searchText.toLowerCase().includes(q);item.hidden=!show;if(show)visible++});document.querySelector('.empty-message').hidden=visible!==0});
document.querySelector('[data-newsletter]')?.addEventListener('submit',e=>{e.preventDefault();e.currentTarget.nextElementSibling.textContent='구독 신청이 완료되었습니다. 다음 편지에서 만나요!';e.currentTarget.reset()});
document.querySelectorAll('[data-auth-form]').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const name=form.dataset.authForm==='signup'?(new FormData(form).get('name')||'민준'):'민준';localStorage.setItem('blog-user',String(name));const msg=form.querySelector('.auth-message');msg.hidden=false;msg.textContent=form.dataset.authForm==='signup'?'가입이 완료되었습니다. 프로필로 이동합니다.':'로그인되었습니다. 글 목록으로 이동합니다.';setTimeout(()=>location.href=form.dataset.authForm==='signup'?'./profile.html':'./index.html',650)}));
const authLink=document.querySelector('.auth-link');if(authLink&&localStorage.getItem('blog-user')){authLink.textContent='내 프로필';authLink.href='./profile.html'}document.querySelector('[data-write-form]')?.addEventListener('submit',e=>{e.preventDefault();const title=e.currentTarget.querySelector('[name=title]').value.trim();const status=document.querySelector('[data-write-status]');if(!title){status.textContent='제목을 입력해주세요.';return}localStorage.setItem('blog-draft',JSON.stringify(Object.fromEntries(new FormData(e.currentTarget))));status.textContent='글이 발행되었습니다. 글 목록으로 이동합니다.';setTimeout(()=>location.href='./index.html',700)});document.querySelector('[data-save-draft]')?.addEventListener('click',()=>{document.querySelector('[data-write-status]').textContent='임시 저장했습니다.'});
